// @ts-check
import { defineConfig } from 'astro/config';

// Site da Sensually — vitrine estática (Astro).
// Ajuste `site` pra URL final quando publicar (Netlify dá uma URL grátis).
export default defineConfig({
  site: 'https://sensually.netlify.app',
});
